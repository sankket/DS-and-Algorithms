# In python dictionaries are implemented as hash table.
# hash function is used for converting the keys into hashes which are unique for each key.
# Hashes are then mapped to the buckets and the key value pair is stored in that bucket.

dictionary = dict()
dictionary = {'one': 1, 'two': 2, 'three' : 3, 'four' : 4}

print(dictionary)

print(dictionary.keys())

print(dictionary.values())


